import { createClient } from '@/lib/supabase/server';

export default async function AdminUsersPage() {
  const supabase = await createClient();
  const { data: users } = await supabase.from('users').select('id, display_name, role, is_onboarded, last_login_at, created_at, updated_at').order('created_at', { ascending: false });
  return <div className="space-y-6"><div><h1 className="text-3xl font-semibold tracking-tight">Users</h1><p className="mt-2 text-sm text-slate-500">Account status, roles, onboarding and last activity across the platform.</p></div><div className="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="border-b border-slate-200 bg-slate-50"><tr><th className="px-5 py-3">User</th><th className="px-5 py-3">Role</th><th className="px-5 py-3">Onboarding</th><th className="px-5 py-3">Last login</th><th className="px-5 py-3">Created</th></tr></thead><tbody className="divide-y divide-slate-100">{(users ?? []).map((user) => <tr key={user.id}><td className="px-5 py-4"><div className="font-medium">{user.display_name || 'Unnamed user'}</div><div className="text-xs text-slate-400">{user.id}</div></td><td className="px-5 py-4"><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold uppercase">{user.role}</span></td><td className="px-5 py-4">{user.is_onboarded ? 'Complete' : 'Pending'}</td><td className="px-5 py-4 text-slate-500">{user.last_login_at ? new Date(user.last_login_at).toLocaleString() : 'Never'}</td><td className="px-5 py-4 text-slate-500">{new Date(user.created_at).toLocaleDateString()}</td></tr>)}</tbody></table></div></div><div className="rounded-xl border border-amber-200 bg-amber-50 p-5 text-sm text-amber-900"><strong>Destructive account actions are intentionally gated.</strong> User deletion should require an explicit email verification/confirmation flow before any account or owned data is removed.</div></div>;
}
